name: Upload store MSIX to release (DISABLED)

permissions:
  contents: write

# DISABLED - This workflow has been disabled
# To re-enable, uncomment the 'on' section below
# on:
#   release:
#     types: [released]   # Run the action when a GitHub release is published
# 
#   schedule:
#     - cron:  '0 */6 * * *'  # Run the action every 6 hours
# 
#   workflow_dispatch:    # Manually run the action

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'
        required: false
        default: 'true'

jobs:
  upload-store-msix-to-release:
    runs-on: ubuntu-latest
    if: false  # Always skip this job

    steps:
    - name: Workflow is disabled
      run: |
        echo "This workflow has been disabled."
        echo "To re-enable:"
        echo "1. Remove or set 'if: false' to 'if: true' in the job"
        echo "2. Uncomment the original 'on' triggers"
        echo "3. Remove the disabled workflow_dispatch input"
        exit 1
    
    # Original steps (disabled):
    # - name: Upload store MSIX to release
    #   uses: hiddify/Upload-Microsoft-Store-MSIX-Package-to-GitHub-Release@develop
    #   with:
    #     store-id: 9pdfnl3qv2s5
    #     token: ${{ secrets.GITHUB_TOKEN }}
    #     asset-name-pattern: Hiddify-Windows-Setup-x64		# Optional